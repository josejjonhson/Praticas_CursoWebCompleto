let logado = false
if(logado) {
    console.log('Logado!')
} else {
    console.log('Acesso negado!')
}

let numBool = 0
console.log(!!numBool)

console.log(!!('' || 0))
console.log(!!(1 && "asdk"))

let = usuario = 'user001'
console.log(usuario || 'root')